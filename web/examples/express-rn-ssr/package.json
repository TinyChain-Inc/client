{
  "name": "@tinychain/web-example-express-rn-ssr",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "bootstrap": "npm --prefix ../.. install",
    "dev": "npm --prefix ../.. run dev",
    "build": "npm --prefix ../.. run build",
    "start": "npm --prefix ../.. run start",
    "test": "npm --prefix ../.. run test",
    "test:unit": "npm --prefix ../.. run test:unit",
    "test:integration": "npm --prefix ../.. run test:integration",
    "test:browser": "npm --prefix ../.. run test:browser"
  }
}
